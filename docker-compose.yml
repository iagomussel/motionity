services:
  app:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - api
    environment:
      - VITE_API_URL=http://api:3001

  api:
    build: ./api
    expose:
      - "3001"
    environment:
      - PORT=3001
