services:
  app:
    build: .
    expose:
      - "80"
    depends_on:
      - api
    environment:
      - VITE_API_URL=http://api:3001
      - VITE_BUILD_ID=local

  api:
    build: ./api
    expose:
      - "3001"
    environment:
      - PORT=3001
